# Notas:

Primer programa en NodeJs

```
Opciones:
      --help     Muestra ayuda                  [booleano]
      --version  Muestra número de versión      [booleano]
  -b, --base     Es la base de la tabla de multiplicar
                                      [número] [requerido]
  -l, --listar   Muesta la tabla en consola
                               [booleano] [defecto: false]
  -h, --hasta    Este es el numero hasta donde queres que
                 valla la tabla     [número] [defecto: 10]
```